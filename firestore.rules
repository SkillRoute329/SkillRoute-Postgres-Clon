rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORAL LOCAL/EMULATOR: permitir todo para E2E y desarrollo con emulador.
    // En producción sustituir por las reglas restrictivas (auth, roles).
    match /{path=**} {
      allow read, write: if true;
    }
  }
}
