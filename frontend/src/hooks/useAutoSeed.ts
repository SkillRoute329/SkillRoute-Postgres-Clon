/**
 * useAutoSeed.ts - DESHABILITADO PERMANENTEMENTE
 * El seed automático ha sido desactivado para evitar datos simulados.
 * Los datos deben cargarse via:
 * 1. Panel de administración manual
 * 2. Importación Excel de UCOT
 * 3. Sincronización GTFS del MTOP
 */

export const useAutoSeed = () => {
  // Auto-seed desactivado - no generar datos falsos
  return;
};
