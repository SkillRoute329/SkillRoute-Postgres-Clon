{
    "name": "transformafacil-monorepo",
    "version": "1.0.1-CACHEBUST-1737694665421",
    "private": true,
    "description": "Root package for Railway deployment - NIXPACKS FORCE",
    "scripts": {
        "install:all": "cd frontend && npm install && cd ../backend && npm install",
        "certify": "node scripts/certify.js",
        "postinstall": "npm run install:all",
        "deploy:prepare": "node scripts/deploy_prepare.js",
        "deploy:verify": "node scripts/verify_remote.js",
        "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate && npm run build",
        "start": "cd backend && node --max-old-space-size=512 dist/index.js",
        "cache:bust": "echo 1737694665421"
    },
    "engines": {
        "node": ">=22.12.0"
    },
    "dependencies": {
        "lodash": "^4.17.21"
    },
    "keywords": [
        "railway"
    ]
}