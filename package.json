{
    "name": "transformafacil-monorepo",
    "version": "12.0.0",
    "private": true,
    "description": "Root package for Railway deployment - NIXPACKS FORCE",
    "scripts": {
        "install:all": "cd frontend && npm install && cd ../backend && npm install",
        "build": "echo 'BUILDING FRONTEND...' && cd frontend && npm install && npm run build && echo 'BUILDING BACKEND...' && cd ../backend && npm install && npx prisma generate && npm run build",
        "start": "cd backend && node dist/index.js"
    },
    "engines": {
        "node": "22.x"
    },
    "keywords": [
        "railway"
    ]
}